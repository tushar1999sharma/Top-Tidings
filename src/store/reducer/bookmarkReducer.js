const initState = {
    currentUser: {},
    isAuthenticated: false,
    headlines: [],
    isLoading: false
}

const bookmarkReducer = (state = initState, action) => {
    
    return state
}

export default bookmarkReducer;